---
import type { ImageMetadata } from 'astro';
import Picture from 'astro/components/Picture.astro';

import TextBox from './TextBox.astro';

import fallbackImage from '../../assets/images/e-community-logo.png';

type Props = {
  image: ImageMetadata;
};

const { image = fallbackImage } = Astro.props;
---

<div class="flex w-full gap-x-5">
  <div class="flex w-full basis-1/4 items-center justify-center bg-amber-100">
    <Picture
      src={image}
      formats={['avif', 'webp']}
      alt="A description of my image."
    />
  </div>
  <div class="basis-3/4">
    <TextBox>
      <slot />
    </TextBox>
  </div>
</div>
